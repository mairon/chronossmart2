<h1 id="header">vendas_cond_liqs</h1>
<div class="acessos">
  <%= link_to t('NEW'), new_vendas_cond_liq_path, :class => "button add" %>
</div>

<table class="head_grid">
  <tr>
            <td>Venda</td>
            <td>Cond liq</td>
      </tr>
</table>

<div class="rolagem" dir="div">
  <table>
    <% @vendas_cond_liqs.each do |vendas_cond_liq| %>
      <tr>
                  <td><%= vendas_cond_liq.venda_id %></td>
                  <td><%= vendas_cond_liq.cond_liq_id %></td>
                <td><%= link_to 'Editar', edit_vendas_cond_liq_path(vendas_cond_liq) %></td>
        <td><%= link_to t('DELETE'), vendas_cond_liq, method: :delete, data: { confirm: 'Esta Seguro?' } %></td>
      </tr>
    <% end %>
  </table>
</div>

