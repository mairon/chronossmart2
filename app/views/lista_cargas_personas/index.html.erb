<h1 id="header">lista_cargas_personas</h1>
<div class="acessos">
  <%= link_to t('NEW'), new_lista_cargas_persona_path, :class => "button add" %>
</div>

<table class="head_grid">
  <tr>
      </tr>
</table>

<div class="rolagem" dir="div">
  <table>
    <% @lista_cargas_personas.each do |lista_cargas_persona| %>
      <tr>
                <td><%= link_to 'Editar', edit_lista_cargas_persona_path(lista_cargas_persona) %></td>
        <td><%= link_to t('DELETE'), lista_cargas_persona, method: :delete, data: { confirm: 'Esta Seguro?' } %></td>
      </tr>
    <% end %>
  </table>
</div>

