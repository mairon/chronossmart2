<%= form_for(@lista_cargas_persona) do |f| %>
  <% if @lista_cargas_persona.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lista_cargas_persona.errors.count, "error") %> prohibited this lista_cargas_persona from being saved:</h2>

      <ul>
      <% @lista_cargas_persona.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<ul class="tabs">
      <li><a href="#tab1"> lista_cargas_personas - [<%= t('EDITION') %>]</a></li>
</ul>

<div class="tab_container">
      <div id="tab1" class="tab_content">
        <table>
          <tr>
            <td width="1000">
              <fieldset>

                <table>
                  <tr>
                                      </tr>                 
                </table>
              </fieldset>
            </td>
          </tr>
        </table>
      </div>
      
      <div class="buttons">
        <%= f.submit :value => t('SAVE'), :class => "button" %>
        <%= link_to t('BACK'), lista_cargas_personas_path, :class => "button back" %>        
      </div>

    </div>
<% end %>