<%= form_for(vendas_pedido) do |f| %>
<div class="panel">
  <div class="panel-content">
    <div class="group">
      <div class="row">
        <div class="col-md-10" >
          <label>Nº Pedido</label>
          <%= f.hidden_field :venda_id %>
          <%= f.collection_select :presupuesto_id, Presupuesto.where(status: 'PENDENTE', persona_id: @venda.persona_id).select('id'), :id, :id, { prompt: 'Selecione o Pedido' }, {required: true} %>
        </div>
        <div class="col-md-2" style="margin-top: 27px">

          <%= f.submit "Grabar", class: "btn btn-green", disable_with: "Aguarde...", "data-plus-as-tab" => "false"%>
        </div>
      </div>
    </div>
  </div>
</div>
<% end %>
<script>
  $('#vendas_pedido_presupuesto_id').selectize();
</script>
