<h1 id="header">Cambio</h1>
<div class="acessos">
  <%= link_to t('NEW'), new_troca_path, :class => "button add" %>
</div>

<table class="head_grid">
  <tr>
            <td width="100"><%= t('DATE') %></td>
            <td width="200">Vendedor</td>
            <td width="200">Observación</td>
      </tr>
</table>

<div class="rolagem" dir="div">
  <table>
    <% @trocas.each do |troca| %>
      <tr>
                  <td width="100"><%= troca.data.strftime("%d/%m/%Y") %></td>
                  <td width="200"><%= troca.persona.nome %></td>
                  <td width="200"><%= troca.observacao %></td>
                  <td><%= link_to 'Show', troca %></td>
                  <td><%= link_to '', edit_troca_path(troca), class: 'icon-pencil', title: 'Modificar' %></td>
                  <td><%= link_to '', troca, method: :delete, class: 'icon-trash', title: t('DELETE'), data: { confirm: 'Esta Seguro?' } %></td>
      </tr>
    <% end %>
  </table>
</div>

