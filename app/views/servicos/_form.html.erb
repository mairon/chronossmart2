<%= form_for(@servico) do |f| %>
  <% if @servico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@servico.errors.count, "error") %> prohibited this servico from being saved:</h2>

      <ul>
      <% @servico.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<ul class="tabs">
      <li><a href="#tab1"> Servicos - [<%= t('EDITION') %>]</a></li>
</ul>

<div class="group">
        <table>
          <tr>
                      <td align="right">Nome :</td>
                      <td><%= f.text_field :nome %></td>

                      <td align="right">Tipo :</td>
                      <td><%= f.text_field :tipo %></td>

                  <tr>
                  </tr>
                      <td>Descricao :</td>
                      <td ><%= f.text_field :descricao %></td>

                      <td align="right">Valor :</td>
                      <td><%= f.text_field :valor %></td>
                  <tr>
                  </tr>

                     <td align="right"> Inicio:</td>
                       <td><%= f.date_select :data_inicio %></td>

                     <td align="right"> Termino:</td>
                       <td><%= f.date_select :data_final %></td>
                  <tr>
                  </tr>
                      <td align="right">Obs :</td>
                      <td colspan="3"><%= f.text_area :obs, cols: "80", rows: "5"%></td>
                </table>
              </fieldset>
            </td>
          </tr>
        </table>

      <div class="buttons">
        <%= f.submit t('SAVE'), :class => "button add", :disable_with => "Aguarde...", "data-plus-as-tab" =>  "false" %>
        <%= link_to t('BACK'), servicos_path, :class => "button back" %>
      </div>

    </div>
<% end %>