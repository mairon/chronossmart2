<%= form_for(@unidades_usuario) do |f| %>
  <% if @unidades_usuario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unidades_usuario.errors.count, "error") %> prohibited this unidades_usuario from being saved:</h2>

      <ul>
      <% @unidades_usuario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<ul class="tabs">
      <li><a href="#tab1"> unidades_usuarios - [<%= t('EDITION') %>]</a></li>
</ul>

<div class="tab_container">
      <div id="tab1" class="tab_content">
        <table>
          <tr>
            <td width="1000">
              <fieldset>

                <table>
                  <tr>
                                      
                      <td align="right">unidade_id :</td>                    
                      <td><%= f.number_field :unidade_id %></td>
                                      
                      <td align="right">usuario_id :</td>                    
                      <td><%= f.number_field :usuario_id %></td>
                                      </tr>                 
                </table>
              </fieldset>
            </td>
          </tr>
        </table>
      </div>
      
      <div class="buttons">
        <%= f.submit :value => t('SAVE'), :class => "button" %>
        <%= link_to t('BACK'), unidades_usuarios_path, :class => "button back" %>        
      </div>

    </div>
<% end %>