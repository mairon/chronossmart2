
 <div class="group">
  <h1 id="header"> Equipo - [<%= t('EDITION') %>]</h1>

<%= form_for(@persona_rodado) do |f| %>
  <% if @persona_rodado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@persona_rodado.errors.count, "error") %> prohibited this persona_rodado from being saved:</h2>

      <ul>
      <% @persona_rodado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-md-3">
      <label>Chapa</label>
      <%= f.text_field :chapa, size: 13, class: "panel-field" %>
    </div>

    <div class="col-md-3">
      <label>Puetas</label>
      <%= f.text_field :portas, class: "panel-field" %>
    </div>
    <div class="col-md-3">
      <label>Color</label>
      <%= f.text_field :cor, class: "panel-field" %>
    </div>

   <div class="col-md-3">
      <label>Año</label>
      <%= f.text_field :ano, class: "panel-field" %>
    </div>
    <div class="col-md-6">
      <label>Marca</label>
      <%= f.text_field :marca, class: "panel-field" %>
    </div>

    <div class="col-md-6">
      <label>Modelo</label>
      <%= f.text_field :modelo, class: "panel-field" %>
    </div>

    <div class="col-md-12">
      <label>Obs</label>
      <%= f.text_area :obs, class: "panel-field", rows: 5 %>
    </div>
   <div class="col-md-12">
      <br>
      <%= f.submit t('SAVE'), class: "btn btn-green", disable_with: "Aguarde...", "data-plus-as-tab" => "false" %>
      <%= link_to t('BACK'), persona_path(@persona_rodado.persona_id) %>
   </div>
  </div>
</div>
<% end %>
