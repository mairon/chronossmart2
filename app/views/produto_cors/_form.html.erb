<%= form_for(@produto_cor) do |f| %>
  <% if @produto_cor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@produto_cor.errors.count, "error") %> prohibited this produto_cor from being saved:</h2>

      <ul>
      <% @produto_cor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<ul class="tabs">
      <li><a href="#tab1"> produto_cors - [<%= t('EDITION') %>]</a></li>
</ul>

<div class="tab_container">
      <div id="tab1" class="tab_content">
        <table>
          <tr>
            <td width="1000">
              <fieldset>

                <table>
                  <tr>
                                      
                      <td align="right">produto_id :</td>                    
                      <td><%= f.number_field :produto_id %></td>
                                      
                      <td align="right">cor_id :</td>                    
                      <td><%= f.text_field :cor_id %></td>
                                      </tr>                 
                </table>
              </fieldset>
            </td>
          </tr>
        </table>
      </div>
      
      <div class="buttons">
        <%= f.submit :value => t('SAVE'), :class => "button" %>
        <%= link_to t('BACK'), produto_cors_path, :class => "button back" %>        
      </div>

    </div>
<% end %>