<%= form_for(@tipo_atividade) do |f| %>
  <% if @tipo_atividade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_atividade.errors.count, "error") %> prohibited this tipo_atividade from being saved:</h2>

      <ul>
      <% @tipo_atividade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<ul class="tabs">
      <li><a href="#tab1"> tipo_atividades - [<%= t('EDITION') %>]</a></li>
</ul>

<div class="tab_container">
      <div id="tab1" class="tab_content">
        <table>
          <tr>
            <td width="1000">
              <fieldset>

                <table>
                  <tr>
                                      
                      <td align="right">nome :</td>                    
                      <td><%= f.text_field :nome %></td>
                                      
                      <td align="right">status :</td>                    
                      <td><%= f.check_box :status %></td>
                                      </tr>                 
                </table>
              </fieldset>
            </td>
          </tr>
        </table>
      </div>
      
      <div class="buttons">
        <%= f.submit :value => t('SAVE'), :class => "button" %>
        <%= link_to t('BACK'), tipo_atividades_path, :class => "button back" %>        
      </div>

    </div>
<% end %>