<%= form_for(@produto_imagen) do |f| %>
  <% if @produto_imagen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@produto_imagen.errors.count, "error") %> prohibited this produto_imagen from being saved:</h2>

      <ul>
      <% @produto_imagen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<ul class="tabs">
      <li><a href="#tab1"> produto_imagens - [Edicion]</a></li>
</ul>

<div class="tab_container">
      <div id="tab1" class="tab_content">
        <table>
          <tr>
            <td width="1000">
              <fieldset>

                <table>
                  <tr>
                                      
                      <td align="right">produto_id :</td>                    
                      <td><%= f.number_field :produto_id %></td>
                                      
                      <td align="right">status :</td>                    
                      <td><%= f.text_field :status %></td>
                                      </tr>                 
                </table>
              </fieldset>
            </td>
          </tr>
        </table>
      </div>
      
      <div class="buttons">
        <%= f.submit :value => " Grabar ", :class => "button" %>
        <%= link_to 'Volver', produto_imagens_path, :class => "button back" %>        
      </div>

    </div>
<% end %>