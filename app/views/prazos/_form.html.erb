<%= form_for(@prazo) do |f| %>
  <% if @prazo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prazo.errors.count, "error") %> prohibited this prazo from being saved:</h2>

      <ul>
      <% @prazo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<ul class="tabs">
      <li><a href="#tab1"> Prazos - [<%= t('EDITION') %>]</a></li>
</ul>

<div class="tab_container">
      <div id="tab1" class="tab_content">
        <table>
          <tr>
            <td width="1000">
              <fieldset>

                <table>
                  <tr>                                      
                    <td align="right">Descripción:</td>                    
                    <td><%= f.text_field :nome, size: 60, autofocus: true %></td>
                                    
                    <td align="right">dias:</td>                    
                    <td><%= f.number_field :dias %></td>
                  </tr>                 
                </table>
              </fieldset>
            </td>
          </tr>
        </table>
      </div>
      
      <div class="buttons">
        <%= f.submit :value => t('SAVE'), :class => "button" %>
        <%= link_to t('BACK'), prazos_path, :class => "button back" %>        
      </div>

    </div>
<% end %>